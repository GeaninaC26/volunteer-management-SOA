{
  "private": true,
  "workspaces": [
    "campaigns",
    "host",
    "registration",
    "scheduling"
  ],
  "scripts": {
    "build": "lerna run build",
    "dev": "lerna run --parallel dev",
    "serve": "lerna run --parallel serve"
  },
  "devDependencies": {
    "lerna": "8.1.8"
  }
}
